name: Import Rebuilding Day One Folder

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import-folder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Main branch
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Clone legacy repository
        run: |
          git clone --depth 1 https://github.com/k4lm3d/nsg.git legacy

      - name: Create destination directory
        run: |
          mkdir -p screenshots

      - name: Copy folder
        run: |
          rm -rf screenshots/rebuilding-day-one
          cp -R legacy/content/news/rebuilding-day-one screenshots/rebuilding-day-one/

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add screenshots/rebuilding-day-one

          git commit -m "Import rebuilding-day-one folder from legacy repository" || echo "No changes to commit"

          git push origin HEAD:main
